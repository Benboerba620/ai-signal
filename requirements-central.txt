# Central feed-generation dependencies (GitHub Actions only).
# Users do NOT need these — user-side scripts only need requirements.txt.
# Note: twscrape requires Python >= 3.10.
-r requirements.txt
# Temporary authenticated XClientTxId fix from upstream PR #321.
# Pin the commit so scheduled runs remain reproducible until a release includes it.
twscrape @ git+https://github.com/jonsuh/twscrape.git@6f2e01d1aa3ee2185809870204e76d12fb65314b
youtube-transcript-api>=0.6
yt-dlp>=2024.1
